# OS junk
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Editor/IDE
.vscode/
.idea/
*.swp
*.swo

# Env / secrets (this is a static site — nothing should normally land here,
# but keep this as a guard rail if tooling or scripts are added later)
.env
.env.*
*.local

# Logs
*.log
npm-debug.log*

# Dependencies (in case build tooling is added later)
node_modules/

# Raw/working photo exports — only the processed files in photos/ ship on the site
Photos for report/
